# ── Core scientific stack ──────────────────────────────────────────────────
numpy>=1.24
scipy>=1.11
matplotlib>=3.7
h5py>=3.9

# ── PyTorch (CPU/MPS for Mac dev; Colab uses cu12 extras via physicsnemo) ──
torch>=2.2
torch-geometric>=2.4          # GNN support (MeshGraphNet dependency)

# ── PhysicsNeMo ────────────────────────────────────────────────────────────
# On Colab: pip install "nvidia-physicsnemo[cu12,nn-extras]"
# On Mac (dev/debug only, no CUDA):
nvidia-physicsnemo>=0.9; sys_platform != "darwin"

# ── Bayesian optimisation ──────────────────────────────────────────────────
botorch>=0.10
gpytorch>=1.11

# ── Geometry & mesh ───────────────────────────────────────────────────────
pyvista>=0.43
trimesh>=4.0
numpy-stl>=3.0

# ── OpenFOAM post-processing ───────────────────────────────────────────────
fluidfoam>=0.2

# ── Streamlit app ─────────────────────────────────────────────────────────
streamlit>=1.32
stpyvista>=0.0.9               # PyVista inside Streamlit
plotly>=5.18

# ── Utilities ─────────────────────────────────────────────────────────────
scikit-learn>=1.3
tqdm>=4.66
pyyaml>=6.0
huggingface-hub>=0.21          # AirfRANS download
datasets>=2.17                 # CFDBench / HuggingFace datasets
